# DEPRECATED: This workflow is no longer used.
# The data branch has been eliminated - update.yml now fetches directly from Hetzner API
# and incrementally updates the DuckDB database.
#
# Keeping this file for reference only. It can be deleted once the new approach is verified.

name: "[DEPRECATED] Fetch Hetzner Auction data"

on:
  # Schedule removed - no longer runs automatically
  # - cron: "13 * * * *"

  # Can still be triggered manually if needed for debugging
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated notice
        run: |
          echo "This workflow is deprecated."
          echo "Use 'Update Database (Incremental)' workflow instead."
          echo "The data branch is no longer needed."
          exit 1
